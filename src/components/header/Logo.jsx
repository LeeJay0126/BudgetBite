

const Logo = () =>{

    return (
        <div className="LogoContainer">
            <h1 className="Logo">
                SHIFT SIMPLE
            </h1>
        </div>
    );
};

export default Logo;